const defaultState = {
    p1Ch1: '',
    p1CH2: '',
    p2CH1: '',
    p2CH2: '',
}

export default (state = defaultState, action) => {
    switch (action.type) {
        // case "DEFAULT":
        // return !state
        default:
        return state;
    }
};